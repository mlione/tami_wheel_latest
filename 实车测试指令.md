# TAMI Wheel DWA + LQR 实车测试指令

> 适用工程：`~/tami/tami_wheel_latest`
> 适用版本：当前 DWA + LQR v0.3 工作区  
> 用途：实车现场快速查阅、分阶段测试、记录数据和排查问题

> **安全警告**：轮椅是载人设备。本手册中的软件安全层不能替代物理急停、人工安全员和底盘原生制动。首次测试必须空载、低速、开阔场地进行，并保证人员可以立即切断驱动电源。未完成空载验收前禁止载人。

## 1. 最常用命令速查

### 1.1 每个新终端都要执行

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
```

如果忘记执行 `source install/setup.sh`，常见现象包括：

- 找不到 `wheel_perception`；
- `/perception/output` 的消息类型无效；
- 运行到了旧版本节点或旧参数。

### 1.2 编译当前工程

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
colcon build
source install/setup.sh
```

当前环境的 `colcon` 可能不支持 `--packages-select`，因此现场优先使用普通的 `colcon build`。

修改 C++、launch、消息定义或 YAML 后都建议重新编译并重新 source。主 launch 从 `install/wheel_perception/share/wheel_perception/config/params.yaml` 加载参数，不能只修改源码 YAML 后继续运行旧的 install 内容。

### 1.3 启动实车相机、感知和规划，但不驱动底盘

```bash
ros2 launch wheel_perception run_launch.py
```

这条命令启动：

- ZED 相机；
- TensorRT 语义分割；
- 点云与道路边界融合；
- DWA 局部规划；
- LQR 跟踪；
- 软件 Safety Layer。

它会发布 `/cmd_vel`，但**不会自动启动蓝牙底盘桥**。这是实车首次检查时最安全的运行方式。

### 1.4 启动 RViz

另开终端：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
rviz2 -d install/wheel_perception/share/wheel_perception/rviz/dwa_navigation.rviz
```

RViz 的 `Fixed Frame` 应为：

```text
base_link
```

### 1.5 最后才启动蓝牙底盘桥

只有在“不连接底盘的检查”全部通过后，才另开终端执行：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 launch ble_hardware_bridge ble_hardware_bridge.launch.py
```

如需覆盖蓝牙参数，先修改：

```bash
ble_hardware_bridge/ble_hardware_bridge/config/ble_hardware_bridge.yaml
```

桥接通过 BlueZ D-Bus 按 `device_address` 和 `characteristic_uuid` 查找设备，不再使用固定 GATT 句柄。更换轮椅或蓝牙模块时必须重新核对这两项。

## 2. 实车运行前必须检查的参数

参数文件：

```text
src/wheel_perception/config/params.yaml
```

### 2.1 确认使用真实 ZED

```yaml
zed:
  use_dataset_mode: false
```

实车模式必须是 `false`。数据集回放时则使用 `true`，且绝对不要同时启动蓝牙底盘桥。

### 2.2 确认 TensorRT engine 路径

当前参数使用绝对路径：

```yaml
ai:
  engine_path: "/home/x/tami/wheel_latest/src/wheel_perception/config/combined5.engine"
```

将工程复制到开发板后，如果用户名或目录变化，必须修改该路径。TensorRT engine 与 GPU 架构、CUDA 和 TensorRT 版本有关；桌面显卡生成的 engine 不保证能在 Jetson 上使用。

### 2.3 当前工作区中需要重新标定的实车参数

当前值包括：

```yaml
lqr:
  gain: 80.0

dwa:
  max_velocity: 1.0
  cruise_velocity: 0.6
  max_angular_velocity: 1.0
  minimum_turning_radius: 0.2
  robot_radius: 0.45
  road_margin: 0.15

safety:
  emergency_stop_distance: 0.3
  perception_timeout: 0.5
```

这些是当前调试值，不应直接视为载人安全参数：

- `max_velocity=1.0 m/s` 和 `cruise_velocity=0.6 m/s` 对首次实车测试偏快；
- `minimum_turning_radius=0.2 m` 可能小于轮椅实际舒适转弯半径；
- `emergency_stop_distance=0.3 m` 对有制动延迟的载人设备通常偏小；
- `robot_radius=0.45 m` 必须覆盖车轮、扶手、脚踏板和乘员；
- `/cmd_vel` 现在使用标准物理单位，不再存在 `lqr.gain` 协议放大参数。

首次空载测试建议先把速度限制在低速范围，例如：

```yaml
dwa:
  max_velocity: 0.20
  cruise_velocity: 0.10
  max_angular_velocity: 0.30
  max_acceleration: 0.15
  max_angular_acceleration: 0.50
```

这只是保守起点，不是对所有轮椅都安全的保证。`robot_radius`、`minimum_turning_radius` 和紧急停车距离应根据实车尺寸、最低可控速度和制动距离测量得到。

修改参数后执行：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
colcon build
source install/setup.sh
```

### 2.4 检查相机外参

当前外参由 `params.yaml` 的 `zed.odometry.extrinsic` 统一管理，
`run_launch.py` 使用同一组数值发布：

```text
base_link -> zed_left_camera_frame
```

当前值为相机相对轮椅中心“前 0.2 m、右 0.2 m”；换算到
ROS `X向前、Y向左` 坐标系后为 `(0.2, -0.2, 0.0) m`。FusionNode
用该外参修正 ZED 位姿、Twist 和协方差。实车安装位置或角度不同时必须重新测量，外参错误会直接造成：

- 障碍点位置偏移；
- 地面或树冠进入 DWA；
- 道路边界方向倾斜；
- RViz 轨迹与真实轮椅方向不一致。

## 3. 推荐的分阶段测试流程

不要直接从“启动全部节点并载人”开始。按下面阶段逐级通过。

## 阶段 A：硬件和环境检查

保持底盘驱动断开或物理急停按下。

### A1. 检查 ZED 是否被识别

```bash
lsusb | grep -i -E 'stereolabs|zed'
```

### A2. 检查蓝牙设备

```bash
bluetoothctl devices
```

需要扫描时：

```bash
bluetoothctl scan on
```

扫描完成后按 `Ctrl+C` 退出。

### A3. 检查 Python 蓝牙依赖

```bash
python3 -c "import bleak; print(bleak.__version__)"
```

如果报 `No module named bleak`，底盘桥会启动但无法连接蓝牙。不要在现场临时安装未知版本后直接载人测试。

### A4. 检查磁盘空间

```bash
df -h ~/tami/tami_wheel_latest
```

录制点云和图像会快速占用空间。

## 阶段 B：只运行感知和规划，不连接底盘

### B1. 启动主系统

终端 1：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 launch wheel_perception run_launch.py
```

正常情况下应看到：

```text
Hierarchical controller ready
Transitioning successful
CRUISE+LQR: ...
```

有足够障碍点进入激活区域时应看到：

```text
Local planner mode: DWA obstacle avoidance
DWA+LQR: ...
```

### B2. 检查节点和生命周期

终端 2：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 node list
ros2 lifecycle get /fusion_node
```

`/fusion_node` 应为：

```text
active
```

### B3. 检查关键话题是否存在

```bash
ros2 topic list | sort
```

至少检查：

```text
/rgb_image
/debug/viz
/perception/output
/zed/point_cloud
/dwa/obstacle_cloud
/dwa/planner_cmd
/dwa/best_path_marker
/dwa/candidate_paths
/cmd_vel
```

### B4. 检查感知频率

```bash
ros2 topic hz /rgb_image
```

另开终端检查：

```bash
ros2 topic hz /debug/viz
```

再检查：

```bash
ros2 topic hz /perception/output
```

不要把终端中 `Performance Profiling` 的 `CURRENT FPS` 直接当成端到端控制频率。该数据主要统计相机、推理、融合和可视化处理时间，不完整包含 DDS 传输、DWA、LQR 与底盘通信延迟。

### B5. 检查感知内容

```bash
ros2 topic echo /perception/output --once
```

重点观察：

- `min_distance` 是否符合真实距离；
- `has_road_edge` 是否稳定为 `true`；
- `right_distance` 是否接近实际距离；
- `road_yaw_error` 在直路上是否接近 `0`，而不是接近 `±3.14`；
- `obstacles` 是否存在明显噪声。

只看道路相关字段：

```bash
ros2 topic echo /perception/output --field road_yaw_error
```

```bash
ros2 topic echo /perception/output --field right_distance
```

### B6. 检查 DWA 与最终控制量

DWA物理速度：

```bash
ros2 topic echo /dwa/planner_cmd
```

最终底盘协议指令：

```bash
ros2 topic echo /cmd_vel
```

二者含义不同：

- `/dwa/planner_cmd.linear.x`：DWA参考线速度，单位 `m/s`；
- `/dwa/planner_cmd.angular.z`：DWA参考角速度，单位 `rad/s`；
- `/cmd_vel.linear.x`：最终线速度；
- `/cmd_vel.angular.z`：经过 LQR 修正和底盘可执行域限幅的最终角速度，单位 `rad/s`。

### B7. 检查里程计反馈

```bash
ros2 topic info /odom -v
ros2 topic hz /odom
ros2 topic echo /odom --field twist.twist
```

实车模式的 ControllerNode 优先读取当前 ZED 驱动发布的 `/odom.twist`。默认反馈超时为 `0.3 s`，超时后回退到上一控制输出并产生警告；如果经过空载验证，可将 `velocity_feedback.stop_on_timeout` 设为 `true`，使反馈超时直接停车。

`/odom.twist` 是 ZED 视觉惯性运动估计，不是轮编码器测得的底盘轮速。必须实测确认直行和转向符号、静止零偏、丢跟恢复和振动影响；未完成验收前仍应保持低速空载。

## 阶段 C：RViz 检查

终端 3：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
rviz2 -d install/wheel_perception/share/wheel_perception/rviz/dwa_navigation.rviz
```

### C1. RViz颜色含义

| 显示 | 含义 |
|---|---|
| 黄色点 | 识别到的道路右边界 |
| 亮绿色最优线 | 无明显障碍时的 `CRUISE+LQR` 巡线参考 |
| 蓝色最优线 | DWA 已启用并选择避障轨迹 |
| 蓝色圆点 | DWA 有有效解，但认为停车轨迹最优 |
| 浅绿色细线 | 有效 DWA 候选轨迹 |
| 红色细线 | 碰撞、越界或动力学不可行的候选轨迹 |
| `/dwa/obstacle_cloud` | 真正参与 DWA 的 ROI 点云 |
| `/zed/point_cloud` | 完整调试点云，不代表全部参与规划 |

### C2. 无障碍巡线验收

直路且前方无障碍时应满足：

- 终端显示 `CRUISE+LQR`；
- 最优轨迹为绿色；
- `/dwa/planner_cmd.angular.z` 接近零；
- `road_yaw_error` 接近零；
- 绿色路径不应大范围左右跳动；
- `/cmd_vel.angular.z` 不应长时间饱和。

如果 `w_ref=0`，但日志中的 `w_track` 长时间为 `±1.0`，问题主要在道路误差或 LQR 跟踪，而不是 DWA 绕障。此时禁止连接底盘。

### C3. 障碍物规划验收

使用稳定、可被深度相机识别的纸箱或泡沫障碍物，不要让人员站在轮椅正前方充当障碍。

先把障碍物放在约 `1.5～2.0 m` 前方，观察：

1. 障碍点是否出现在 `/dwa/obstacle_cloud`；
2. 激活区域内点数是否超过 `activation.minimum_points`；
3. 日志是否从 `CRUISE+LQR` 变为 `DWA+LQR`；
4. 是否出现蓝色最优轨迹或蓝色停车点；
5. 蓝色轨迹是否位于道路边界内；
6. `clearance` 是否大于零且保持合理余量；
7. 障碍移走后，是否经过 `clear_frames` 帧恢复绿色巡线轨迹。

如果出现大量浅绿色候选但没有蓝色线，应查看是否选择了蓝色停车点；如果既没有蓝线也没有蓝点，则检查 ControllerNode 是否仍在运行以及是否发布了有效最优轨迹。

## 阶段 D：单独验证蓝牙桥和控制方向

这一阶段先不要运行 `run_launch.py`，避免 ControllerNode 与人工测试同时发布 `/cmd_vel`。驱动轮应悬空，或者轮椅放在测试架上。

### D1. 启动蓝牙桥

终端 1：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 launch ble_hardware_bridge ble_hardware_bridge.launch.py
```

必须看到：

```text
BLE bridge ready
Connecting to BLE device...
Connected. Writing commands to ...
```

### D2. 发送短时间低速直行指令

终端 2：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.15}, angular: {z: 0.0}}"
```

观察驱动方向后按 `Ctrl+C`，然后立即发送零指令：

```bash
ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.0}, angular: {z: 0.0}}"
```

蓝牙桥默认在超过 `0.3 s` 未收到新 `/cmd_vel` 时发送停车帧，但该超时不能代替物理急停。

### D3. 转向方向测试

当前 `/cmd_vel.angular.z` 单位为 `rad/s`。必须在驱动轮悬空状态从小角速度开始确认方向：

```bash
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.15}, angular: {z: 0.10}}"
```

结束后立即发送零指令。再使用相反符号验证左右方向：

```bash
ros2 topic pub -r 10 /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.15}, angular: {z: -0.10}}"
```

只有确认正负号、停止行为、前后方向和蓝牙断开恢复都正确后，才能进入地面空载测试。

## 阶段 E：完整系统空载低速测试

### E1. 启动顺序

1. 接通 ZED 和计算平台；
2. 物理急停保持可用；
3. 启动 `run_launch.py`；
4. 启动 RViz；
5. 检查感知、道路、障碍点云和控制输出；
6. 最后启动 `ble_hardware_bridge`；
7. 释放物理急停，进行短距离空载测试。

推荐使用四个终端：

终端 1——感知与控制：

```bash
ros2 launch wheel_perception run_launch.py
```

终端 2——RViz：

```bash
rviz2 -d install/wheel_perception/share/wheel_perception/rviz/dwa_navigation.rviz
```

终端 3——观察控制：

```bash
ros2 topic echo /dwa/planner_cmd
```

终端 4——蓝牙底盘桥：

```bash
ros2 launch ble_hardware_bridge ble_hardware_bridge.launch.py
```

### E2. 第一次地面测试内容

按以下顺序，每一步只运行数秒并停车检查：

1. 无障碍直路低速直行；
2. 检查道路右边界跟踪方向；
3. 障碍物放在较远位置，确认 DWA 激活但不急转；
4. 障碍物偏左、偏右分别测试绕行方向；
5. 将障碍物逐渐靠近，但人员不要进入车前；
6. 验证软件紧急停止；
7. 遮挡相机或停止感知节点，验证感知超时停车；
8. 移走障碍，确认恢复巡线时没有突然加速。

### E3. 每次测试必须记录

- 当前 Git 提交或工作区版本；
- `params.yaml` 备份；
- 轮椅是否空载；
- 场地、路宽、地面材质和光照；
- 障碍物尺寸和位置；
- 最大实测速度；
- 是否有真实 `/odom.twist`；
- 是否触发 DWA、停车或感知超时；
- 是否发生左右摆动、急转或路径跳变。

## 4. 录制实车测试数据

### 4.1 轻量控制包

不录完整图像和点云，适合长时间记录控制行为：

```bash
ros2 bag record -o wheel_control_test \
  /perception/output \
  /dwa/planner_cmd \
  /cmd_vel \
  /dwa/best_path \
  /odom \
  /tf \
  /tf_static
```

结束时按 `Ctrl+C`。下次测试应更换输出目录，例如 `wheel_control_test_02`，避免目录已存在导致启动失败。

### 4.2 完整规划调试包

点云数据量较大，只建议短时间录制：

```bash
ros2 bag record -o wheel_dwa_full_test \
  /perception/output \
  /dwa/planner_cmd \
  /cmd_vel \
  /dwa/best_path \
  /dwa/best_path_marker \
  /dwa/candidate_paths \
  /dwa/obstacle_cloud \
  /zed/point_cloud \
  /perception/debug/right_road_edge \
  /odom \
  /tf \
  /tf_static
```

查看包信息：

```bash
ros2 bag info wheel_dwa_full_test
```

回放：

```bash
ros2 bag play wheel_dwa_full_test
```

回放 rosbag 时不要启动蓝牙底盘桥。

## 5. 现场常用诊断命令

### 5.1 查看某个话题的发布者和订阅者

```bash
ros2 topic info /cmd_vel -v
ros2 topic info /dwa/planner_cmd -v
ros2 topic info /perception/output -v
```

完整系统测试时，`/cmd_vel` 应只有 ControllerNode 作为正常控制发布者。若人工 `ros2 topic pub` 仍在运行，会出现竞争控制，必须停止人工发布进程。

### 5.2 查看节点信息

```bash
ros2 node info /controller_node
ros2 node info /fusion_node
```

### 5.3 检查 TF

```bash
ros2 run tf2_ros tf2_echo base_link zed_left_camera_frame
```

如果 RViz 报 TF 错误或点云、道路边界、路径互相错位，优先检查这里。

### 5.4 检查单个消息类型

```bash
ros2 interface show wheel_msgs/msg/PerceptionOutput
ros2 topic type /perception/output
```

如果提示消息类型无效：

```bash
cd ~/tami/tami_wheel_latest
source /opt/ros/humble/setup.bash
source install/setup.sh
ros2 interface show wheel_msgs/msg/PerceptionOutput
```

仍无效时重新编译，再打开新终端 source。

### 5.5 检查是否触发 DWA

主终端日志出现：

```text
Local planner mode: DWA obstacle avoidance
```

或者检查蓝色 Marker：

```bash
ros2 topic echo /dwa/best_path_marker --once
```

激活依据是前方区域内点数达到阈值，不是只看 `min_distance`。紧急停止则优先使用融合最小距离。

## 6. 常见故障与处理

| 现象 | 首先检查 | 处理原则 |
|---|---|---|
| `/rgb_image` 正常但 `/debug/viz` 卡住 | `ros2 topic hz /debug/viz`、TensorRT日志、FusionNode状态 | 先确认推理和 lifecycle 正常，不要连接底盘 |
| `/perception/output` 类型无效 | 当前终端是否 source 工作区 | 重新编译、自定义消息包成功后打开新终端 source |
| RViz 无路径 | `/controller_node`、`/perception/output`、`/dwa/best_path_marker` | 检查节点是否因 YAML 参数类型错误退出 |
| 只有绿色路径 | 前方激活区障碍点少于阈值 | 查看 `/dwa/obstacle_cloud`，不应为了显示蓝线盲目降低阈值 |
| 只有蓝色停车圆点 | DWA 判断停车候选得分最高 | 检查净空、速度权重、噪声点和最小转弯半径 |
| 候选大量为红色 | 碰撞、越界或动力学约束过严 | 检查 `robot_radius`、`road_margin`、道路边界和 ROI |
| 绿线大范围波动 | `road_yaw_error` 或 `right_distance` 波动 | 先修感知，不要先提高 DWA/LQR增益 |
| `w_ref=0` 但 `w_track` 饱和 | LQR看到很大的横向或航向误差 | 禁止上车，检查道路角度、坐标系和外参 |
| 树木出现在完整点云 | 是否也出现在 `/dwa/obstacle_cloud` | 完整点云出现不代表影响DWA；按实际高度调整 DWA ROI |
| 障碍出现但DWA不激活 | 激活区内点数、横向位置和高度 ROI | 检查障碍点是否实际落在激活区域 |
| DWA频繁启停 | 点云噪声、`minimum_points`、`clear_frames` | 先处理点云稳定性，再调整状态滞回参数 |
| 蓝牙不断重连 | 地址、句柄15、权限、距离和供电 | 先单独验证蓝牙桥，禁止边重连边测试车辆 |
| 轮椅运动时 `/odom.twist` 持续为零 | ZED跟踪失效、配置未生效或方向映射错误 | 停止底盘，检查ZED跟踪状态、`zed.odometry.enabled` 和控制器速度来源日志 |
| `Safety stop: perception timeout` 持续出现 | `/perception/output`频率、FusionNode状态 | 感知未闭环，禁止驱动底盘 |

## 7. 停车和紧急情况处理

### 7.1 正常结束

1. 先让轮椅实际停稳；
2. 保持物理急停可用；
3. 停止蓝牙桥终端；
4. 停止主 launch；
5. 停止 RViz 和 rosbag；
6. 检查 rosbag 是否完整写入。

### 7.2 出现异常运动

优先级如下：

1. 使用物理急停或切断底盘驱动；
2. 不要依赖鼠标寻找终端；
3. 轮椅确认停稳后再按 `Ctrl+C` 停止 ROS 节点；
4. 保存终端日志和 rosbag；
5. 在数据集或悬空状态复现，禁止直接继续载人试验。

不要把以下操作当成可靠的紧急停车：

- 临时执行一次 `ros2 topic pub --once`；
- 关闭 RViz；
- 等待蓝牙桥 1 秒 watchdog；
- 等待 DWA 下一帧重新规划。

控制器运行时会持续发布 `/cmd_vel`，额外发布一次零速度可能立即被下一条控制指令覆盖。

## 8. 实车验收清单

每一项都确认后才能进入下一测试等级。

### 感知与坐标

- [ ] ZED 图像、深度和点云连续更新；
- [ ] `base_link -> zed_left_camera_frame` 是实测外参；
- [ ] 直路 `road_yaw_error` 接近零；
- [ ] 右边界距离和实际测量一致；
- [ ] `/dwa/obstacle_cloud` 不包含明显树冠、地面和孤立噪声；
- [ ] 紧急距离测量经过卷尺验证。

### 规划与控制

- [ ] 无障碍时稳定显示绿色巡线轨迹；
- [ ] 障碍物达到阈值时切换为蓝色DWA轨迹或蓝色停车点；
- [ ] 所选路径没有越过道路边界；
- [ ] 最小转弯半径符合实车能力；
- [ ] `/dwa/planner_cmd` 没有严重逐帧跳变；
- [ ] `w_track` 不会在直路长期饱和；
- [ ] 感知丢失和无有效路径时能够停车。

### 底盘与安全

- [ ] 蓝牙地址、GATT句柄和发送频率正确；
- [ ] 线速度正方向正确；
- [ ] 角速度正负方向正确；
- [ ] 蓝牙断开后底盘进入安全状态；
- [ ] 物理急停可在任何软件状态下停车；
- [ ] 已测量不同速度下的实际制动距离；
- [ ] 已完成悬空、空载低速、空载避障三级测试；
- [ ] 测试现场有安全员和无人员侵入区。

## 9. 当前实现限制

实车测试结论必须考虑以下限制：

- DWA 使用二维恒定 `(v,w)` 运动学预测，不包含质量、轮胎侧滑和电机延迟；
- 轮椅外形当前近似为圆，而不是真实多边形 footprint；
- 当前只处理静态障碍点，不预测行人运动；
- 少量近距离噪声点可能主导 DWA 的最小净空；
- ZED `/odom.twist` 链路已打通，但尚未连接实车完成数值方向、漂移和丢跟恢复的系统级验收；
- 当前静态相机 TF 是零位姿占位；
- 当前 YAML 中速度、转弯半径和紧急停车距离仍是调试值；
- `/cmd_vel` 已保持标准 ROS SI 单位，具体蓝牙协议映射在独立 `ble_hardware_bridge` 中完成。

因此，当前工程适合开展受控场地中的空载、低速、静态障碍物规划验证，还不能仅凭数据集和 RViz 结果直接认定满足载人安全要求。
